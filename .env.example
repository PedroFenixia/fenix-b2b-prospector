# ============================================================
# FENIX Prospector — Environment Variables
# Copy to .env and fill in production values
# ============================================================

# --- Domain (used by Caddy for auto-SSL) ---
DOMAIN=b2b.fenixia.tech

# --- Database (PostgreSQL in Docker) ---
POSTGRES_PASSWORD=CHANGE_ME_strong_random_password
DATABASE_URL=postgresql+asyncpg://fenix:${POSTGRES_PASSWORD}@postgres:5432/fenix_prospector

# --- Typesense search engine ---
TYPESENSE_API_KEY=CHANGE_ME_random_key
TYPESENSE_URL=http://typesense:8108
TYPESENSE_COLLECTION=companies

# --- Auth / Security ---
SECRET_KEY=CHANGE_ME_long_random_string
ADMIN_PASSWORD=CHANGE_ME_admin_password
DEMO_PASSWORD=CHANGE_ME_demo_password

# --- Data directories ---
DATA_DIR=data
BORME_PDF_DIR=data/borme_pdfs
EXPORT_DIR=data/exports

# --- BOE API ---
BOE_API_BASE=https://boe.es/datosabiertos/api

# --- Ingestion ---
PDF_DOWNLOAD_CONCURRENCY=15
SCHEDULER_ENABLED=true
SCHEDULER_HOUR=10
SCHEDULER_MINUTE=0

# --- CIF Enrichment ---
APIEMPRESAS_KEY=
ENRICHMENT_PROXIES=

# --- Stripe (pagos) ---
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_PRO=
STRIPE_PRICE_ENTERPRISE=
APP_URL=https://b2b.fenixia.tech

# --- RevenueCat (suscripciones) ---
REVENUECAT_API_KEY=
REVENUECAT_WEBHOOK_AUTH=
REVENUECAT_ENTITLEMENT_PRO=pro
REVENUECAT_ENTITLEMENT_ENTERPRISE=enterprise

# --- Email (SMTP) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@fenixia.tech
SMTP_FROM_NAME=FENIX Prospector
